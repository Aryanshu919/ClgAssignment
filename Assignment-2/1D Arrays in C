#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int no = 0;
scanf("%d",&no);
int arr[no];    
int sum =0;
for(int i=0; i<no; i++){
    scanf("%d",&arr[i]);
}
for(int i =0; i<no; i++){
    sum += arr[i];
}
printf("%d",sum);
    return 0;
}
